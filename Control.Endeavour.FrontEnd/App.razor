@* <Router AppAssembly="@typeof(App).Assembly">
    <Found Context="routeData">
        <RouteView RouteData="@routeData" DefaultLayout="@typeof(MainLayout)" />
        <FocusOnNavigate RouteData="@routeData" Selector="h1" />
    </Found>
    <NotFound>
        <PageTitle>Not found</PageTitle>
        <LayoutView Layout="@typeof(MainLayout)">
            <p role="alert">Sorry, there's nothing at this address.</p>
        </LayoutView>
    </NotFound>
</Router> *@

<Router AppAssembly="@typeof(App).Assembly">
    <Found Context="routeData">
        <AuthorizeRouteView RouteData="@routeData" DefaultLayout="@typeof(LoginLayout)">
            <Authorizing>
                <div>
                    <row class="centered-element container-fluid">
                        <h6>
                            <span>Autorizando...</span>
                        </h6>
                    </row>
                </div>
            </Authorizing>
            <NotAuthorized>
                <div>
                    <row class="centered-element container-fluid">
                        <h6>
                            <span>No está Autorizado...</span>
                        </h6>
                    </row>
                </div>
            </NotAuthorized>
        </AuthorizeRouteView>
    </Found>
    <NotFound>
        <CascadingAuthenticationState>
            <LayoutView Layout="@typeof(NotFoundLayout)">





                <p align="center" role="alert" class="NotFound--paragraphStyle"> Lo sentimos, la pagina que buscas no se encuentra disponible. <br />Su nombre cambio o esta temporalmente inhabilitada. </p>

  
            </LayoutView>
        </CascadingAuthenticationState>
    </NotFound>
</Router>